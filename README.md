A Persona is a small brain that feels → expresses → observes → changes slightly.
Persona
 ├── Traits (numbers)
 ├── Memory (effects)
 ├── Express() → LLM (speaker)
 └── Perceive() → LLM (listener)
          ↓
     Suggested changes
          ↓
     System applies bias rules
